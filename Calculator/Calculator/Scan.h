#include <string>
#include <queue>
#include <stack>
using namespace std;
extern queue <string> Que;
extern stack <double> Num;
extern stack <string> Str;

class Scan
{
public:
	void ToStringQueue(string input);//处理输入的字符串 并传入队列
};
